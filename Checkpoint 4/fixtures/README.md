# Fixture payloads (reference)

Offline Scout candidates are embedded in `phase3_enrichment_cmds.py` (`FIXTURE_SCOUT_CANDIDATES`).

`--fixture-full` writes them to `phase3_results/*/agent_bus/source_candidates.json` and forces the NTEE acquisition plan so the full bus runs without network.
